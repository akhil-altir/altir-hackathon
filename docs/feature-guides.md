# Changes-1
- We should be able to mark few users as Admin and they should have admin prevliges, Judges might ahve a similar login and experience to reduce complexity. 
- For the admin user, create a page to add api keys -- where we can add API keys and assign it to different teams.
- Admin should be able to define the Event Scores which will be used for calculations
