[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
# Run db push to sync schema, then start Next.js
startCommand = "npm start"
sleepApplication = true
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
