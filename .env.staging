# Staging — PostgreSQL on Railway (staging service)
# Set DATABASE_URL in Railway dashboard for the staging environment.
# Do NOT commit real credentials here.
DATABASE_PROVIDER="postgresql"
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/altir_tech_day_staging?schema=public"
