# Production — PostgreSQL on Railway
# Set DATABASE_URL in Railway dashboard for the production environment.
# Do NOT commit real credentials here.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/altir_tech_day?schema=public"

# NOTE: before deploying, update prisma/schema.prisma:
#   datasource db { provider = "postgresql" ... }
# and run: npx prisma migrate deploy
